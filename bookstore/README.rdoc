== README

Note to people trying to use ActiveAdmin:

1.  Rememeber to run `rake db:seed` to get your first AdminUser

2.  If you get the error `nodenv: version 'system' is not installed`, you need to run `brew install node`.  Running `brew install nodenv` didn't help.

